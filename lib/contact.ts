// TODO(open item — CONTEXT.md "Open pre-release items" #4): real contact
// details (email / phone / WhatsApp / address / city) are not yet confirmed.
// Do NOT hardcode a guessed value here — confirm with Jimmy before go-live.
// Left empty so callers can conditionally hide the mailto/tel links rather
// than silently showing a fake address.
export const CONTACT_EMAIL = "";
export const CONTACT_PHONE = "";
export const CONTACT_ADDRESS = "";
